import styled from "styled-components"

export const Aside = styled.aside`
  background-color:#FDCD6B;
  border-radius: 1rem;
  padding: 1rem;
  line-height: 2rem;
  h3 strong{
    font-weight: bold;
  }
`